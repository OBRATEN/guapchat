class Dialogue {
  String id;
  String user_id;
  String username;
  String lastMessageText;
  String lastMessageDate;
  Dialogue(this.id, this.user_id, this.username, this.lastMessageText, this.lastMessageDate);
}